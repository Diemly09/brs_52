<%= form_for @user, url: user_path(@user), remote: true,
  html: {method: :put} do |f| %>
  <%= render "shared/errors_messages", object: @user %>

  <div class="form-group">
    <%= f.label :email %>
    <%= f.email_field :email, autofocus: true, disabled: true, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :Tên %>
    <%= f.text_field :name, autofocus: true, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :password %>
    <i>(<%= t "users.noti_password" %>)</i>
    <%= f.password_field :password, autocomplete: "off", class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :password_confirmation %>
    <%= f.password_field :password_confirmation, class: "form-control"  %>
  </div>
  <%= f.submit "Cập Nhật", class: "btn btn-primary" %>
<% end %>
